** DB schema changed to allow hashed passwords, changed DB schema on server **

- sending cookies from the client is not safe

- validation for username password length etc
- ListAll is automatic in the UI - consider changing
- no Admin functionality
- figure out presentation requirements/who explains what
- prevent SQL injection?
- implement proper http response codes in /LAMPAPI
- make sure API responses are consistent (success always goes in top)